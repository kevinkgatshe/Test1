#include <Servo.h>
Servo servo1, servo2, servo3, servo4;
int angle1, angle2, angle3, angle4;


void setup() {
  servo1.attach(3);
  servo2.attach(5);
  
 
  // put your setup code here, to run once:

}

void loop() {
  for(angle1 = 0; angle1 < 90; angle1 ++){
    servo1.write(angle1);
    delay(80);
  }

  for(angle2 = 0; angle2 < 180; angle2++){
    servo2.write(angle2);
    delay(45);
  }
  
  for(angle1 = 90; angle1 > 0; angle1--){
    servo1.write(angle1);
    delay(80);
  }

  for(angle2 = 180; angle2 > 0; angle2--){
     servo2.write(angle2);
     delay(45);
  } 
 

  
  // put your main code here, to run repeatedly:

}

---------------------------------------

#include <Servo.h>
Servo servo1, servo2, servo3, servo4;
int angle1, angle2, angle3, angle4;


void setup() {
  servo1.attach(3);
  servo2.attach(5);
  servo3.attach(9);
  servo4.attach(10);
  
 
  // put your setup code here, to run once:

}

void loop() {
  for(angle1 = 0; angle1 < 90; angle1 ++){
    servo1.write(angle1);
    delay(80);
  }


  for(angle3 = 0; angle3 < 180; angle3++){
    servo3.write(angle3);
    delay(70);
  }

  for(angle4 = 0; angle4 < 180; angle4++){
    servo4.write(angle4);
    delay(70);
  }

  
  for(angle2 = 0; angle2 < 180; angle2++){
    servo2.write(angle2);
    delay(45);
  }

  
  for(angle1 = 90; angle1 > 0; angle1--){
    servo1.write(angle1);
    delay(80);
  }

  for(angle3 = 180; angle3 > 0; angle3++){
    servo3.write(angle3);
    delay(70);
  }

  for(angle4 = 180; angle4 > 0; angle4++){
    servo4.write(angle4);
    delay(70);
  }

  for(angle2 = 180; angle2 > 0; angle2--){
     servo2.write(angle2);
     delay(45);
  } 
